<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.learnable.learnable">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/download"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ICDLActivity"></activity>
        <activity android:name=".SQLActivity" />
        <activity android:name=".PHPActivity" />
        <activity android:name=".JqueryActivity" />
        <activity android:name=".BootStrapActivity" />
        <activity android:name=".ExcelActivity" />
        <activity android:name=".GitActivity" />
        <activity android:name=".LaravelActivity" />
        <activity android:name=".WordpressActivity" />
        <activity android:name=".JavaActivity" />
        <activity android:name=".CssActivity" />
        <activity android:name=".HTMLActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>